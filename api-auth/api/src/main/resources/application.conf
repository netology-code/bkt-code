ktor {
    deployment {
        port = 8888
        port = ${?PORT}
    }
    application {
        modules = [ ru.netology.ApplicationKt.module ]
    }
}

ncraft {
    upload {
        dir = ./uploads
        dir = ${?UPLOAD_DIR}
    }
}
